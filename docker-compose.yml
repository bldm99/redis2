version: '3.8'

services:
  api:
    build:
      context: ./Api
      dockerfile: Dockerfile
    ports:
      - "5000:80"  # Puedes ajustar el puerto según tu configuración
    depends_on:
      - react

  react:
    build:
      context: ./react
      dockerfile: Dockerfile
    ports:
      - "3000:80"  # Puedes ajustar el puerto según tu configuración
